// Write a function that takes in an array and returns a new array with all numbers multiplied by 3.
// var testArr1 = [3, 9, 15, 4, 10]
//
// Expected output --> [9, 27, 45, 12, 30]

// Declare a function
const multiplyArray = (array) => {
  var newArray = [];
  //Create a for loop to go through the arg array
  for (let i = 0; i < array.length; i++) {
    //in the four loop we are going to multiply each number by three and store inside a new array
    array[i]

  }
  // return the new array

}
